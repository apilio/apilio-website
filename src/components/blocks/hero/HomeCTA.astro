---
// Home page Hero Section
// ------------
// Description: Hero section with headline, subheadline, CTAs and hero image.

// Components
import Section from '../../ui/Section.astro'
import Row from '../../ui/Row.astro'
import Col from '../../ui/Col.astro'
import { Image } from 'astro:assets'
import Button from '../../ui/Button.astro'

// Content
import heroImage from '../../../assets/hero-app-screenshot.png'
---

<Section id="intro" padding="top" classes="bg-neutral-50 dark:bg-neutral-900">
	<Row>
		<Col span="2" />
		<Col span="8" align="center">
			<h1>The logic layer your smart home is <strong>missing</strong></h1>
			<p class="pb-0 text-lg">
				Your devices are powerful. But simple if-then rules hold them back. Apilio lets you build
				real automation logic — across brands, no hub required.
			</p>
			<div class="flex flex-wrap items-center justify-center gap-4">
				<Button size="lg" link="https://app.apilio.com/">Start free trial</Button>
				<Button size="lg" link="#how-it-works" style="neutral" variation="outline">
					See how it works
				</Button>
			</div>
		</Col>
		<Col span="2" />
		<Col span="1" />
		<Col span="10">
			<Image
				src={heroImage}
				alt="Apilio dashboard showing devices, conditions, logicblocks, and event logs"
				class="translate-y-8 rounded-lg shadow-2xl shadow-neutral-200 dark:shadow-neutral-950"
				format="webp"
			/>
		</Col>
		<Col span="1" />
	</Row>
</Section>
